import 'package:expiry_cart/categories_helper/category.dart';

class Products extends Category {

  Products({
    String name,
    String imgName,
  }) : super(
          imgName: imgName,
          name: name,
        );
}
